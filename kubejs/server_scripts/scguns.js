ServerEvents.highPriorityData((event) => {
	/*let jsonFolder = global.readJsonFolderFromMod('data', 'scguns', 'guns');
	for (let [datapath, _json] of Object.entries(jsonFolder)) {
		_json = JsonIO.toObject(_json);

        if (!_json.projectile.gravity && _json.projectile.item != "scguns:microject" && _json.projectile.item != "scguns:rocket") {
            _json.projectile.gravity = true;
            _json.projectile.speed = _json.projectile.speed * 1.5;
            _json.projectile.life = _json.projectile.life * 5;
        }

        for (let i = 0; i < 5; i++) {
            if (!_json['projectile_' + i]) break
            if (!_json['projectile_'+i].gravity && _json['projectile_' + i].item != "scguns:microject" && _json['projectile_' + i].item != "scguns:rocket") {
                _json['projectile_' + i].gravity = true;
                _json['projectile_' + i].speed = _json['projectile_' + i].speed * 1.75;
                _json['projectile_' + i].life = _json['projectile_' + i].life * 5;
            }
        }

		event.addJson(datapath, _json);
	}

    jsonFolder = global.readJsonFolderFromMod('data', 'scguns_cnc', 'guns');
	for (let [datapath, _json] of Object.entries(jsonFolder)) {
		_json = JsonIO.toObject(_json);

        if (!_json.projectile.gravity && _json.projectile.item != "scguns:microject" && _json.projectile.item != "scguns:rocket") {
            _json.projectile.gravity = true;
            _json.projectile.speed = _json.projectile.speed * 1.5;
            _json.projectile.life = _json.projectile.life * 5;
        }

        for (let i = 0; i < 5; i++) {
            if (!_json['projectile_' + i]) break
            if (!_json['projectile_'+i].gravity && _json['projectile_' + i].item != "scguns:microject" && _json['projectile_' + i].item != "scguns:rocket") {
                _json['projectile_' + i].gravity = true;
                _json['projectile_' + i].speed = _json['projectile_' + i].speed * 1.75;
                _json['projectile_' + i].life = _json['projectile_' + i].life * 5;
            }
        }

		event.addJson(datapath, _json);
	}*/
});
